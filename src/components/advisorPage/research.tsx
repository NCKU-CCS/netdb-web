import React from 'react';
import classNames from 'classnames';

const Research: React.FC = () => (
  <div className={classNames('advisorpage-research')}>advisor research</div>
);

export default Research;
