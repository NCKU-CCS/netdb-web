import React from 'react';
import classNames from 'classnames';

const Patents: React.FC = () => (
  <div className={classNames('advisorpage-patents')}>advisor patents</div>
);

export default Patents;
