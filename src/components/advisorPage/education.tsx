import React from 'react';
import classNames from 'classnames';

const Education: React.FC = () => (
  <div className={classNames('advisorpage-education')}>advisor education</div>
);

export default Education;
